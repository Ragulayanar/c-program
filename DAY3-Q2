PRODUCT OF LIST
#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    double product = 1.0;
    int val;

    for (int i = 0; i < n; i++) {
        scanf("%d", &val);
        product *= val;
    }

    printf("The Product of list value is %.2lf\n", product);

    return 0;
}
